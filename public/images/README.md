# Image Assets

Save the user-provided images here with these names so the site can load them:

- `gloss-git-logo.jpg` - imported from the public Luma event cover.
- `rooftop-group.jpg` - the wide rooftop group photo.
- `workshop-speakers.jpg` - the speaker/workshop photo.
- `dessert-table.jpg` - the dessert and flowers photo.

The React page has graceful CSS fallbacks for the three optional photos, so the site still renders if those files are not present yet.
